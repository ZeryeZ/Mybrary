<!-- <-% isnt outputting anything its just saying that this is js code but doesnt put anything on the view model-->
<!-- <-%-= is returning the value into the template(html escaped) -->

<% if (locals.errorMessage != null) {%>
    <%= errorMessage %> 
<% } %>